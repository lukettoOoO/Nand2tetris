class Array {

    function Array new(int size)
    {
        var Array arr;
        let arr = Memory.alloc(size);
        return arr;
    }

    method void dispose()
    {
        do Memory.deAlloc(this);
        return;
    }
}