class Test {
    static int count;
    field boolean flag;

    constructor Test new(int size) {
        var Array a;
        let a = Array.new(size);
        return this;
    }

    method void setFlag(boolean value) {
        let flag = value;
        return;
    }

    function void compute() {
        var int i, sum;
        let i = 0;
        let sum = 0;

        while (i < 10) {
            let sum = sum + i;
            let i = i + 1;
        }

        if (sum > 20) {
            do Output.printString("Sum is large");
        } else {
            do Output.printString("Sum is small");
        }

        let count = sum;

        do Output.printInt(count);
        return;
    }

    function void testArray() {
        var Array a;
        var int x;
        let a = Array.new(2);
        let a[0] = 10;
        let x = a[0];
        do Output.printInt(x);
        return;
    }
}