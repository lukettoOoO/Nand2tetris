class Example {
    field int x;
    static boolean flag, flag1;

    function void doNothing() {
        var int x;
        var int y;
        
        // Assign a value to an array element - assuming x is an array
        let x[0] = 0;

        // if-statement with condition
        if (x < y) {
            let x = 1;
        } else {
            let x = 2;
        }

        // while-statement with condition
        while (x < 10) {
            let x = 3;
        }

        // do-statement calling a subroutine
        do Output.printInt(x);

        return;
    }
}